services:
  default:
    image: ${IMAGE_NAMESPACE?}:/${IMAGE_NAME?}:${IMAGE_VERSION?}
    build:
      args:
        - COMPOSER_IMAGE_URL
        - NGINX_IMAGE_URL
        - NODE_IMAGE_URL
        - WDQSQUERYGUI_COMMIT
      tags:
        - ${IMAGE_NAMESPACE?}/${IMAGE_NAME?}:latest
        - ${IMAGE_NAMESPACE?}/${IMAGE_NAME?}:${IMAGE_VERSION_MAJOR?}
        - ${IMAGE_NAMESPACE?}/${IMAGE_NAME?}:${IMAGE_VERSION_MINOR?}
