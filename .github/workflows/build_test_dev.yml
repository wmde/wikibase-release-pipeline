name: 🏗️🧪 Build and Test Dev Version

on:
  push:
    branches:
      - "main"
  pull_request:
    paths-ignore:
      - "docs/**"
      - "**/*.md"
      - "**/*.txt"

jobs:
  dev:
    uses: ./.github/workflows/_build_test.yml
