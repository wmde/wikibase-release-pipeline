# Wikibase Suite configuration

This directory contains configuration files used by Wikibase Suite (WBS). Some files are generated during first setup. Back up this folder regularly, because it can contain local changes, passwords, and generated service configuration.

Find information on how to run WBS in the [top-level README](../README.md).
