[DEFAULT]
NAME = wikibase

[index]
INPUT_FILES = wikibase.nt
MULTI_INPUT_JSON = [{ "cmd": "cat wikibase.nt", "format": "nt", "parallel": "true" }]
SETTINGS_JSON = { "ascii-prefixes-only": false, "num-triples-per-batch": 10000 }
STXXL_MEMORY = 256M

[runtime]
SYSTEM = native
