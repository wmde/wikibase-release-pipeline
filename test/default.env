## Test Environment Setup

# These values override values set from `../example/template.env`

## Test specific

# Used for tests to find their way to Docker instances running in test
# TODO: Can this be passed or inferred another way to reduce configurable noise?
DOCKER_WIKIBASE_REPO_NAME=test-wikibase-1
DOCKER_WIKIBASE_CLIENT_NAME=wikibase-client-1
DOCKER_MYSQL_NAME=test-mysql-1

WIKIBASE_CLIENT_HOST=wikibase-client.svc
WIKIBASE_CLIENT_PORT=8383
WIKIBASE_CLIENT_URL=http://wikibase-client.svc

## URL Values from known hostnames
WDQS_FRONTEND_URL=http://wdqs-frontend.svc
WDQS_URL=http://wdqs.svc:9999
PINGBACK_BEACON_URL=http://mediawiki.svc
WDQS_PROXY_URL=http://wdqs-proxy.svc

## Overrides from Example defaults

# TODO: Do we want / need this on for our tests? Was before so left it.
MAX_JOBS=4
MW_ENABLE_UPLOADS=true
QUICKSTATEMENTS_PUBLIC_URL=http://quickstatements.svc
WIKIBASE_URL=http://wikibase.svc
WIKIBASE_PUBLIC_URL=http://wikibase.svc
