## DEFAULT Test Environment Setup
# These values override values set from `../example/template.env`
DB_SERVER=mysql.svc:3306
DB_USER=wikiuser
DB_PASS=sqlpass
MW_ADMIN_NAME=WikibaseAdmin
MW_ADMIN_PASS=WikibaseDockerAdminPass
MW_ADMIN_EMAIL=admin@example.com
MW_WG_SECRET_KEY=secretkey
# TODO: Do we want / need this on for our tests? Was before so left it.
MW_WG_ENABLE_UPLOADS=true
# TODO: This disables ElasticSearch in tests (which gets overridden in the ElasticSearch tests). Explore further.
MW_ELASTIC_HOST=

WIKIBASE_PORT=8181
MAX_JOBS: 2

# Test specific -- Used for tests to find their way to Docker instances running in test
# TODO: Can this be passed or inferred another way to reduce configurable noise?
DOCKER_WIKIBASE_REPO_NAME=test-wikibase-1
DOCKER_MYSQL_NAME=test-mysql-1

