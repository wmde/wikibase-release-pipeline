version: '3.4'

services:

  mysql:
    volumes:  
      - ./suites/client-create-db.sql:/docker-entrypoint-initdb.d/02-client-create-db.sql

  wikibase:
    volumes:
      - ./suites/repo-LocalSettings.php.template:/LocalSettings.php.template
      - ./suites/repo-extra-install.sh:/extra-install.sh

  wikibase-client:
    image: "${WIKIBASE_TEST_IMAGE_NAME}"
    links:
      - mysql
      - wikibase
    depends_on:
      - mysql
      - wikibase
    restart: always
    volumes:
      - ./suites/client-LocalSettings.php.template:/LocalSettings.php.template
      - ./suites/client-extra-install.sh:/extra-install.sh
      - ./suites/${SUITE}/results:/var/log/mediawiki
    ports:
      - "80:80"
    networks:
      default:
        aliases:
         - wikibase-client.svc
    environment:
      - DB_SERVER
      - MW_ADMIN_NAME=WikibaseAdmin
      - MW_ADMIN_PASS=WikibaseDockerAdminPass
      - MW_ADMIN_EMAIL=admin@example.com
      - MW_WG_SECRET_KEY=secretkey
      - DB_USER=wikiuser
      - DB_PASS=sqlpass
      - DB_NAME=client_wiki
