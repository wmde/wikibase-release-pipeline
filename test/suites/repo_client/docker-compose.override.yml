version: '3.5'

services:

  mysql:
    volumes:  
      - ./client-create-db.sql:/docker-entrypoint-initdb.d/02-client-create-db.sql

  wikibase:
    volumes:
      - ./repo-LocalSettings.php.template:/LocalSettings.php.template
      - ./repo-extra-install.sh:/extra-install.sh

  wikibase-client:
    image: "${WIKIBASE_TEST_IMAGE}"
    links:
      - mysql
      - wikibase
    depends_on:
      - mysql
      - wikibase
    restart: always
    volumes:
      - ./client-LocalSettings.php.template:/LocalSettings.php.template
      - ./client-extra-install.sh:/extra-install.sh
      - ./${SUITE}/results:/var/log/mediawiki
    ports:
      - "80:80"
    networks:
      default:
        aliases:
         - wikibase-client.svc
    environment:
      - DB_SERVER
      - MW_ADMIN_NAME=WikibaseAdmin
      - MW_ADMIN_PASS=WikibaseDockerAdminPass
      - MW_ADMIN_EMAIL=admin@example.com
      - MW_WG_SECRET_KEY=secretkey
      - DB_USER=wikiuser
      - DB_PASS=sqlpass
      - DB_NAME=client_wiki
