# Wikibase For testing
version: '3.4'

services:
  wikibase-test:
    build:
      context: ../Docker/test/curl/
      dockerfile: Dockerfile
    environment:
      - QUICKSTATEMENTS_HOST
      - WDQS_FRONTEND_HOST
      - WDQS_HOST
      - WDQS_PORT
      - WIKIBASE_HOST
      - WIKIBASE_PORT
      - SKIP_WDQS